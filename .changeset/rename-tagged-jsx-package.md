---
"tagged-jsx-dom-expressions": patch
---

Rename the prerelease `sld-dom-expressions` package to `tagged-jsx-dom-expressions`, with matching public API names: `createTaggedJSXRuntime`, `TaggedJSXInstance`, and the `.jsx` self-reference tag.
